<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kkuil.blackchat.mapper.ContactMapper">
    <select id="getCursorPage" resultType="com.kkuil.blackchat.domain.bo.contact.ContactWithActiveMsg">
        SELECT c.uid,
               c.room_id,
               c.read_time,
               r.active_time,
               r.last_msg_id
        FROM contact AS c
                 JOIN room AS r
                      ON c.room_id = r.id
        WHERE uid = #{uid}
        <if test="request.cursor">
            AND r.active_time > #{request.cursor}
        </if>
        ORDER BY r.active_time DESC
        LIMIT 0, #{request.pageSize}
    </select>
</mapper>
